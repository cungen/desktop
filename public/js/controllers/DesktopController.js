angular
    .module('controllers')
    .controller('DesktopController', [
        '$scope',
        'LocalStorageService',
        '$rootScope',
        function($scope, LocalStorageService, $rootScope) {

        }
    ]);
